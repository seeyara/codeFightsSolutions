boolean containsDuplicates(int[] a) {
    int l=a.length;
    Set<Integer> set=new HashSet<Integer>();
    for(int i=0;i<l;i++){
        if(set.contains(a[i]))
            return true;
    else set.add(a[i]);
    }
    return false;
}
